// expect that if a user don't belong to ActionBar, it renders blocked;
// LATER : should test if I try to send a message, take a photo or send a file the respective hook is called;
// hooks will be tested individually later with forced uploads and not from the components which uses it;
// Capturing files or uploading relies on Browser API which are third partys we don't test.

